// backend/models/User.js

const express = require('express');
const router = express.Router();

// Assuming this is a model file, the post route should not be defined here.
// It seems like there is a mistake in the file path mentioned in the error message.

// If you are defining routes in the User model file, make sure they are defined properly.
// For example:
router.post('/', (req, res) => {
  // Handle POST request logic here
});

module.exports = router;
